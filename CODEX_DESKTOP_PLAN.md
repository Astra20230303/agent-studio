# Codex Desktop 复刻：功能梳理与执行计划

## 1. 调查结论

### 已从参考图确认的界面骨架

- 顶部桌面窗口栏：侧栏开关、前进/后退、文件/编辑/视图/帮助、窗口控制。
- 左侧工作区：Codex 工作区菜单、搜索、通知、新对话、Pull Request、已安排、插件、项目列表、最近会话、账户/更新入口。
- 中央新对话：欢迎标题、四类快捷提示卡片。
- 底部 Composer：项目选择、文本输入、附件、访问权限、模型/推理强度、发送。
- 视觉语言：浅灰系统背景、白色主工作区、细边框、圆角卡片、低对比度文字、蓝色未读提示。

### 当前环境可验证的产品能力面

- 会话/线程：创建新任务、继续任务、读取状态、等待任务、归档、置顶、重命名。
- 项目：按项目启动任务，区分本地运行与 worktree，读取项目列表与 Git 状态。
- 协作：Pull Request/review 入口、任务之间发送后续消息、线程分支/交接。
- 自动化：heartbeat 与 cron 两类 recurring automation，支持暂停、恢复、失败通知策略。
- 工具与工作区：本地终端、浏览器标签、文件展示、工作区依赖加载。
- 定制化：Skills、Plugins、MCP、项目配置、hooks、模型与 reasoning effort。

### 尚未被官方网页直接验证的内容

当前网络无法连接 `developers.openai.com`，因此本项目不声称复刻了 Codex Desktop 的内部实现或未公开 API。以下内容按截图和当前可调用能力做产品级模拟：真实模型调用、账户同步、GitHub 网络操作、沙箱执行、自动化调度后端。

## 2. 复刻范围

### P0：必须完成

1. 桌面壳层与响应式布局。
2. 新对话、消息时间线、输入框、快捷提示、发送状态。
3. 最近会话列表、搜索、未读状态、新对话重置。
4. 项目选择器与当前项目状态。
5. Pull Request、已安排、插件、设置等导航页面的可视化状态。
6. 浅色/深色主题、通知、侧栏折叠、键盘快捷键。
7. 无后端时使用本地存储保存会话、主题和项目选择。

### P1：下一阶段增强

1. 会话重命名、置顶、归档和删除确认（本地演示状态，根原型已完成；React 版接口待迁移）。
2. 模型选择与 reasoning effort 下拉菜单（根原型与 React 版已完成）。
3. 附件队列和拖拽上传的本地预览（React 版已接入 turn/start、turn/steer 和排队输入；完整模型端到端效果取决于渠道）。
4. Pull Request 列表、review diff 占位视图（根原型已完成，React 版有列表入口）。
5. 已安排页面：创建、暂停、恢复、失败通知策略（根原型已完成，React 版有暂停入口）。
6. 插件/Skill/MCP 卡片与启用状态（根原型与 React 版已有本地能力卡片）。

### P2：真实后端接入（已接入，持续加固）

1. `ThreadStore`、项目状态、草稿/附件/队列和自动化状态已由 React 状态层与 Electron 持久化桥接承载；仍需继续抽离为独立异步 store 接口。
2. 真实线程、回合流、审批、用户补充信息、上下文用量/压缩、恢复与中断已通过 Codex app-server JSON-RPC 接入；渠道端到端效果取决于配置的模型服务。
3. Git 状态/分支/工作树/差异/冲突/提交、终端、文件浏览、网页搜索、浏览器标签和本机/远程 Computer Use 已接入 Electron IPC 或 MCP；仍需扩大系统级窗口和真实目标环境覆盖。
4. 权限、错误边界、离线重试、原生存储恢复、通知和危险 Git 操作保护已有实现与测试；审计日志、签名打包、多平台发布仍是后续工作。

## 3. 执行计划

### 阶段 A：信息架构与视觉基线（已完成）

- [x] 单文件可运行原型。
- [x] 参考图布局、颜色、间距、卡片和 Composer。
- [x] 新对话、快捷卡片、侧栏折叠、菜单、通知、深浅主题。
- [x] 本地 HTTP 运行与浏览器 DOM/交互验证。

### 阶段 B：工作流状态化（进行中）

- [x] 用户消息与模拟助手消息时间线。
- [x] 搜索最近会话与 `Ctrl/Cmd + K`。
- [x] 主题持久化。
- [x] 导航页统一渲染器（Pull Request、已安排、插件、设置）。
- [x] 会话、项目、主题和附件的 localStorage 持久化。
- [x] 模型/推理强度与附件状态面板。

### 阶段 C：页面完整度

- [x] Pull Request 页面。
- [x] 已安排页面。
- [x] 插件页面。
- [x] 设置页面（关于入口仍为演示反馈）。
- [x] 项目选择页面（当前项目与最近项目为演示数据）。

### 阶段 D：验证与交付

- [x] 1280px 默认 viewport 截图校验；CSS 已覆盖窄屏断点。
- [x] 键盘 Enter/Ctrl-K/Escape 验证，按钮保持原生可达。
- [x] P0/P1 交互 DOM 断言：消息、导航页、搜索、项目、主题、模型、附件、设置。
- [x] 无外部静态资源依赖；本地 HTTP 页面加载正常。
- [x] 记录真实后端未接入项与替换点。
- [x] Electron + React + TypeScript + Vite 迁移骨架与生产构建验证。

## 4. 技术决策

当前环境默认没有系统 Node/npm，因此根目录保留零依赖 HTML/CSS/原生 JavaScript 验证原型，保证可双击或通过任意静态 HTTP 服务运行。同时已新增 `desktop/` 工程化迁移骨架，目标技术栈为 Electron + React + TypeScript + Vite：Electron 对应桌面窗口与本地能力，React 管理多页面状态，TypeScript 约束线程/项目/自动化数据模型，Vite 提供开发与构建流程。工作区依赖中可找到 Node/pnpm 时，可在 `desktop/` 安装依赖并继续迁移。

## 6. 真实后端替换点

| 当前演示模块 | 当前实现 | 正式接入替换点 |
| --- | --- | --- |
| 会话/线程 | `localStorage` + 本地模拟助手回复 | `ThreadStore`：创建、读取、继续、归档、置顶、重命名、消息流 |
| 项目 | 静态 `my-agent-plantform` 选项 | `ProjectStore`：项目列表、Git 状态、worktree/local 运行环境 |
| Pull Request | 本地 PR 卡片与 diff 模态框 | GitHub/GitLab connector + review/diff API |
| 已安排 | 本地任务卡片与弹窗 | `AutomationStore`：heartbeat/cron、状态、通知策略、运行历史 |
| 插件 | 浏览器控制/工作区依赖静态卡片 | Plugin/Skill/MCP registry 与启用状态 |
| 模型 | 本地模型选择文本 | 模型目录、reasoning effort 能力和账户权限 |
| 附件 | 本地文件名队列 | 工作区文件选择、上传/沙箱挂载、大小与权限校验 |
| 通知 | 页面 toast | 桌面通知、失败提醒与任务唤醒 |

正式接入时应先定义这些接口，再把 UI 组件从 `localStorage` 迁移到异步 store；UI 不应直接依赖具体 connector。

## 5. 验收标准

- 用户可以从欢迎页发起消息并看到用户/助手状态。
- 用户可以切换并搜索最近会话，点击后恢复输入上下文。
- 用户可以切换项目、主题和导航页面；刷新后主题/项目/会话仍在。
- 侧栏、菜单、搜索、快捷键在常见 viewport 下均可操作。
- P0 功能有可复现的本地验证记录；所有未接入后端的功能明确标注为演示状态。

## 7. 当前交付状态

- 根目录零依赖原型：P0/P1 本地演示路径已完成并验证。
- `desktop/` Electron + React + TypeScript + Vite：工程骨架、状态模型、欢迎页、Composer、消息流、导航页和生产构建已完成并验证。
- 未完成项：独立异步 store 抽象、完整审计日志、Electron 打包签名、多平台发布和覆盖所有真实目标环境的系统级窗口测试。线程/模型流、Git、自动化、终端、浏览器、文件和 Computer Use 已有可运行接入与分层验收，但不应将有限环境测试描述成所有渠道和平台均已验证。

- 项目操作记录修正：原生项目 ID 是路径，切换事件改为仅记录动作；历史详情清理及失败重试已验证（749c26b）。完整审计覆盖仍待逐项核对。
- Agent 关联会话导航：由已加载协作记录生成返回入口，已验证多来源、草稿隔离和重载（f35874e）；不等同于完整服务端任务拓扑。
- 会话恢复失败重试（fa5c6e4）：失败期间阻止发送并保留草稿，支持重试及迟到响应隔离；队列回归通过。
- 通知设置失败恢复（146b153）：读取和保存均可原地重试，保留确认值，校验响应，UI 与持久化回归通过。
- 项目键盘入口（cd6ca39）：命令面板按项目名称/路径新建会话，保留原草稿；项目、工作树绑定与命令回归已验收。
- 归档恢复保护（05a4ab0）：独立恢复锁跨重连保持，恢复中禁止关闭，失败可重试；并发与归档流程已验证。
- 会话列表边界校验（465b766）：普通/归档分页共享校验，异常页保留原列表并可重试，损坏展示字段不进入会话状态。
- 侧栏工作区副标题及窄窗口布局（95d497b、92f9a32）：同名区分、可访问描述、标题栏尺寸保护；完整侧栏验收已闭合。
- 用户消息原文复制（12a37c3）：保留正文格式，键盘操作、失败重试与空消息保护已验证。
- 会话查找输入法保护（8378546）：组合输入期间屏蔽 Enter/Escape 查找动作，失焦/关闭后恢复；查找、历史加载、全局快捷键及构建验证通过。原生输入法实机矩阵仍待验证。
- 独立异步存储第一阶段（1ead359）：底层适配器可注入、实例隔离，原生顺序落盘/退出 flush 与队列同步确认保留；单测、浏览器失败恢复和真实 Electron 迁移/重启验收通过。领域级 ThreadStore 等接口仍待抽离。
- 会话读取失败保护（4847aaf）：损坏/不可读状态阻止主界面自动保存，原始数据保留，修复后可重试恢复；浏览器故障注入与真实 Electron 恢复回归通过。
- 异步应用状态仓库（24184f5）：StateRepository 承接异步读取/保存，启动验证快照直接交给 App，消除重复读取；异步确认、数据保护及真实 Electron 恢复通过。完整领域服务接口仍待继续。
- 删除会话确认与重试（2ed779c）：等待服务端成功后关闭，处理中阻止重复操作，失败保留会话与错误；离线、取消焦点、快捷键隔离及侧栏回归通过。
- 归档并发与当前会话保护（7acaafc）：统一入口、按会话请求锁、失败重试和迟到响应隔离；浏览器并发/持久化与真实 app-server 归档恢复通过。
- 归档/删除队列生命周期（ae3cad7）：mutation 前持久化暂停，保存失败不请求；迟到回合事件不派发，归档保留队列、删除成功清理；专项与队列回归通过。
- 损坏队列保护（2f37a2c）：整份校验失败暂停读写，保留原始记录供修复重试；混合坏条目、正常派发及归档/删除生命周期回归通过。
- 技能选择保存重试（f58e228）：最新选择和清空操作均可原地重试；跨会话、刷新恢复、技能发送/追加/队列回归通过。
- 技能草稿读取保护（e9888bc）：坏记录暂停写入，修复重试合并当前编辑与其他会话记录；真实界面入口、保存重试和技能发送回归通过。
- 共用草稿存储（9eb6ffd）：四类草稿共用持久化与恢复；消息/附件/插件补齐坏记录保护和合并重试。浏览器专项、发送清理与真实 Electron 恢复验收通过。
- 置顶操作记录（9385646）：侧栏/命令面板共用动作记录，不含标题正文路径；刷新、搜索及会话记录回归通过。
- 停止请求按回合隔离（d5fa15c）：等待锁、持续失败提示与重试、跨会话迟到反馈隔离；新回合和队列回归通过。
- 真实回合中断验收（55ff8e5）：实际 app-server + Felix 客户端封装验证流式中断、同会话继续与历史恢复；外部工具进程终止仍待专项验证。
- 后台命令管理（094bef0）：按会话刷新/分页/逐项终止，失败重试与会话隔离；真实 app-server + Windows PowerShell PID 退出及后续会话继续已验证。
- 后台命令资源快照（13bf95f）：PID/CPU/RSS 展示，缺失指标明确未知，刷新更新；单测、界面与真实 app-server 回归通过。
- 后台命令自动刷新（4a27cc6）：展开与重连更新、旧响应隔离、操作锁跨连接保留；读取和终止跨重连专项通过。
- 操作记录读取恢复（1c8f630）：保留损坏原始数据、重试读取后恢复记录及历史脱敏；审计搜索/清空/保存重试回归通过。
- 压缩请求跨会话保护（ce4e468）：锁和反馈跟随会话，切换不重复请求；浏览器回归与真实服务端压缩摘要续聊通过。
- 审批原生模态（2940f12）：背景交互隔离、提交期间焦点保护与 Escape 无隐式决策；审批和快捷键回归通过。
- 多审批队列验收（29a8db9）：解决通知推进、重复请求去重、迟到成功/失败隔离及焦点通过，无产品修正。
- 文件审批差异（9c10859）：按会话/回合/工具条目匹配并实时展示补丁，缺失明确提示；验收修正文件展开项键盘焦点循环，隔离单测、窄屏和审批队列回归通过。真实写入审批全过程待专项验证。
- 真实文件审批（979b734）：实际 app-server 的审批前补丁经 Felix 状态处理准确匹配；允许才落盘、拒绝不写入、工具状态和审批解决通知均通过，补齐前轮真实写入链路验证。使用本地固定模型响应，云模型兼容性仍独立验证。
- 后台命令终止记录（de370d5）：服务端确认终止才记录，不保存命令/路径；跨会话及重连迟到成功、失败/已退出不记成功、刷新搜索和记录读取恢复回归通过。
- 应用状态生命周期抽离（21fa03e）：主界面改用可注入仓库的 useDesktopState；自动保存、重试及不可变更新集中管理，迟到响应、存储恢复和真实 Electron 回归通过。领域级 store 继续推进。
- 异步会话变更服务（99a4f58）：重命名/归档/删除接入可注入服务，远端确认后更新目标本地会话；领域单测、并发队列、界面和真实归档协议回归通过。完整 ThreadStore 仍待整合。
- 归档恢复服务整合（e7907cb）：弹窗等待统一异步恢复服务，保留已有历史与当前选择；快照隔离、离线恢复、重连锁和真实 app-server 服务调用通过。
- 完整历史读取服务（15847c6）：查找和导出统一调用可注入分页服务，异常游标/循环保护、失败不返回部分结果；单测、界面及真实服务逐条分页验收通过。
- 历史条目损坏保护（6ba5bb7）：自动恢复及完整历史加载先校验，异常保留消息与草稿；修复重试、去重、附件/未知工具和导出回归通过。
- 自动化读取仓库（70bee8c）：列表/详情统一校验后进入页面，异常保留已有数据并可重试；真实调度器界面与损坏响应注入验收通过。保存/运行/取消/删除/状态接口已于 c5d11a6 整合，07d8243 将请求互斥下沉仓库以跨页面保留。
- 自动化变更接口（c5d11a6）：保存/运行/取消/删除/状态切换统一调用仓库，提交同步锁防止重复请求；延迟确认、失败重试、真实调度器界面及构建验收通过。
- 任务仅失败通知（a7147ca）：新增任务级通知范围，成功/主动停止静默、失败通知；兼容旧任务和总开关，调度器持久化及编辑/重启界面验收通过。
- 任务通知导航（040bc42）：点击通知打开对应任务详情；验收修正旧跳转重放，覆盖重复点击/已删除/草稿保留。操作系统通知中心点击仍待原生实测。
- 任务结果会话入口（a62e598）：有 threadId 的运行记录可直接打开并恢复对话，复用已有会话及恢复重试；原草稿保留、重复导航和任务页面回归通过。
- 任务会话真实持久化（7cefcde）：修正 ephemeral 和独立历史目录导致入口无法恢复的问题，持久会话共用应用目录、临时文件仍独立；第二个真实 app-server 恢复和失败/中断 ID 保留通过。
- 真实任务续聊（ccd18fb）：第二个 app-server 恢复任务会话后完成下一回合，模型获得原上下文；带密钥/本地免密、回合 ID 和调度记录隔离验收通过。
- 任务会话渠道绑定（536be4a）：执行前持久化实际渠道/模型，恢复不随全局渠道漂移；绑定失败阻止模型回合，真实任务与渠道路由回归通过。
- 自动化推理强度（41bf06d）：任务可选默认/低/中/高并实际传入执行回合；真实模型请求参数、旧任务默认、保存重启和提醒兼容验收通过。
- 固定间隔自动化（b1907b9）：新增每隔 N 分钟，保存/恢复重新计时，错过多次只补跑一次；调度器与编辑/重启界面验收通过。
- 自动化管理记录（d2801fd）：七类管理动作服务确认后记录，不含任务内容；失败/重复提交和真实调度器全流程验收通过。
- 项目选择仓库（8caba9c）：统一选择与默认目录校验，异常不污染当前会话；修复重选、命令面板项目绑定及任务界面回归通过。
- 移除最近项目（a0c4281）：菜单移除仅清理最近列表，先固化已有会话目录；验收修正空列表占位，草稿/目录保留和项目回归通过。
- 模型目录响应校验（d4a343c）：聊天和任务共用严格校验及去重，异常可刷新并保留选择；损坏响应、禁止保存、重试及过期响应隔离验收通过。模型推理能力元数据仍待接入。
- 渠道配置与迁移目录校验（654e3a5）：统一拒绝损坏目录，阻止错误配置和会话迁移；选择恢复、手动模型及渠道续聊验收通过，修正过期的相对目录测试夹具。
- 聊天模型默认强度（8a995f1）：新增可持久化默认状态，恢复默认不再固定低强度；会话/队列实际参数、规划回归及真实 app-server 高强度后清除覆盖验收通过。
- 远端默认强度恢复（2d6e037）：协议 null 恢复为模型默认，不误继承全局强度；保留本地选择、失败重试及真实服务返回值验收通过。
- 工作区服务重启记录（7f26d1b）：按实际请求/停止结果记录，防重复且不误报成功；失败重试、刷新持久化及草稿/连接时序验收通过。
- 复制任务（0642437）：详情复制到可编辑新草稿，保留配置并隔离 ID/状态/历史；权限确认、取消、真实调度器独立保存和重启验收通过。
- 最近失败任务筛选（58995a4）：按最新运行结果筛选，支持搜索及键盘导航；真实调度器失败/后续运行更新和完整任务界面回归通过。
- 未保存任务编辑保护（bc3a17b）：取消/关闭/Escape 确认放弃，保存锁和焦点恢复；时间修改、复制和真实调度器界面验收通过，跨退出草稿恢复仍待实现。
- 任务列表排序（f4410c6）：按下次运行、最近运行或名称排列，支持筛选搜索；时区、稳定顺序、动态刷新及真实调度器界面回归通过。
- 任务详情配置（678faae）：直接展示渠道、推理强度和通知策略；默认/提醒兼容及真实调度器保存重启后的显示验收通过。
- 运行配置快照（84f9f45）：运行开始保存任务执行配置，历史可展开查看；编辑隔离、失败、重启和损坏数据兼容验收通过。动态默认的实际解析结果仍待记录。
- 实际执行环境（a1bcaed）：模型启动前持久化已解析渠道 ID 与目录，运行详情可查；真实请求前写入、失败/重启保留、写入失败阻止执行及界面回归通过。
- 历史运行导出（30477e1）：复制/导出包含运行时名称、配置及实际环境；改名隔离、旧记录兼容、真实任务复制/导出一致性验收通过。
- 任务执行时限（d83f4ca）：支持 1–120 分钟及默认十分钟，接入执行/详情/复制/快照/导出；参数、重启、真实执行超时回调与界面验收通过。
- 原生历史恢复校验（c4592d5）：启动先校验运行/快照/环境与重复 ID，再执行恢复写入；原文件保护、旧记录、真实执行与任务界面验收通过。
- 任务存储原位重试（9d918de）：启动读取失败后，修复文件即可从列表重试恢复，无须重启；缺失/损坏文件保护及真实调度器界面验收通过。
- 自动化实时输出（99deea3）：运行中详情展示输出，合并刷新并在终态保存；真实执行器、停止/重启及迟到回调验收通过，未落盘预览不保证崩溃恢复。
- 实时输出定期保存（5461f35）：两秒合并写盘，失败提示并自动重试，结束取消定时器；快照中断恢复、实际计时及任务界面回归通过。
- 任务会话 ID 提前保存（b734c37）：创建及绑定后、模型执行前保存会话入口，异常退出恢复可用；真实请求时序、写入失败阻止执行和界面回归通过。
- 终态写盘失败恢复（f7969f1）：保留最终结果并重试，落盘前阻止新运行，完成通知一次；三类终态及真实执行器/界面回归通过。
- 任务历史查找（7afec34）：按结果和内容搜索运行历史，支持动态刷新及匹配数量；历史配置/会话 ID、无结果、重开及真实任务回归通过。
- 长输出截断提示（0545720）：实时/终态持久化截断标记，详情与导出明确提示；真实超长输出、重启和界面回归通过。
- 任务列表传输优化（2d72f00）：高频列表省略历史配置和环境正文，详情仍完整；样本列表减少约 82.5%，任务搜索/复制/详情和真实调度器回归通过。

- 任务详情刷新合并（f50d109）：连续通知合并为一次后续读取，慢响应仍可展示，旧任务响应隔离；失败重试、关闭迟到响应、历史搜索及真实调度器界面验收通过。

- 独立会话查询仓库（cf6ba96）：普通/归档列表与内容搜索共用异步接口和数据校验；实例组装独立于协议层，损坏结果重试、界面回归和真实 app-server 双范围内容搜索验收通过。完整 ThreadStore 仍待整合。

- 恢复会话快照校验（ae83209）：应用恢复数据前确认会话身份、回合结构与配置字段，失败保留原状态并阻止发送；故障注入、真实 app-server、历史去重和排队回归通过。

- 旧消息分支定位（ff96391）：分页查找校验数据并拒绝循环游标，失败解锁可重试；浏览器故障注入、分支回归和真实 app-server 定位验收通过。

- 历史加载进度与取消（561ba04）：分页显示进度，支持立即取消、切换会话取消及原位重试，已有消息保留；挂起/迟到响应、查找/导出与真实服务回归通过。

- 会话导出进度与取消（547a6b6）：读取阶段显示进度并支持取消/切换自动取消，部分记录不保存；迟到响应、重试、保存阶段边界和历史查找回归通过。

- MCP 状态载荷校验（29060c8）：分页服务状态、工具、资源和模板进入 UI 前校验并深拷贝；坏数据保留旧列表可重试，MCP 全套界面回归与构建通过。

- 工作区文件响应校验（8b4c546）：文件浏览/搜索/预览桥接载荷进入 UI 前校验并深拷贝，坏响应不渲染；单测、工作区界面和生产构建通过。

- 后台命令响应校验（990502b）：分页和终止载荷进入终端面板前校验，坏进程条目不覆盖现有列表；后台命令完整界面回归与生产构建通过。

- Git 状态与差异校验（9fe0b99）：仓库状态/文件/差异桥接响应进入面板前校验并深拷贝，坏载荷阻止 Git 操作；Git 界面回归和生产构建通过。

- Git 提交历史校验（bcb7a2f）：历史分页、提交条目和详情进入 UI 前校验，重复/坏载荷可重试；历史界面与真实 Git 回归、生产构建通过。

- Git 分支响应校验（1334612）：分支列表、远端引用和提交 ID 进入 UI 前校验，坏快照阻止 Git 操作；分支/工作树回归与生产构建通过。

- Git 工作树响应校验（772b7db）：工作树列表载荷进入 UI 前校验并深拷贝，坏记录不渲染；创建/重开/删除保护回归和生产构建通过。

- Git 冲突版本校验（4f64a6a）：冲突基线/当前/传入阶段载荷进入 UI 前校验，坏数据可重试且不执行文本；冲突界面回归和生产构建通过。

- 已有工作树打开保护（c8b814c）：校验项目载荷，兼容主工作区 local 环境；坏路径不污染状态、重试与工作树重开验收通过。

- 未产生首个提交的分支兼容（72da5d6）：真实 Git 验证新仓库、首个提交及孤立分支；8 项测试、三项分支浏览器回归和生产构建通过。验收修正远端跟踪/删除测试的提交 ID，补充空仓库界面及切换忙碌保护断言。

- 当前增量计划：补齐从当前提交创建本地分支并切换，保留工作区修改；原生校验名称、重名与过期快照，覆盖游离 HEAD 和首个提交前保护。实现后验证提交，再进行界面验收并提交修正。

- 本地分支创建已交付（952f4f8）：3 项真实 Git 测试、真实 Git 界面验收、原有三项分支界面回归通过；验收改为显式动作类型并修正刷新等待断言。

- 当前增量计划：批量暂存/取消暂存，覆盖新增、修改、删除及首个提交前状态；拒绝过期分支/提交与未解决冲突。先验证原生 Git 并提交，再连接真实仓库进行界面验收和修正提交。

- 批量暂存已交付（056807a）：6 项真实 Git 测试、真实 Git 浏览器完整提交流程、原写入界面回归与生产构建通过；验收修正操作等待时关闭/刷新/历史导航入口保护。

- 当前增量计划：以应用级 ThreadStore 统一列表/搜索与重命名/归档/恢复/删除，并注入侧栏和归档页；按本地和远端身份互斥写操作，保留原有队列、离线和选择生命周期保护。单测及真实 app-server 验证后提交，再做浏览器回归验收。

- 应用级 ThreadStore 已交付（91dcaaf）：统一查询/搜索和手动生命周期写操作，按会话身份互斥；14 项单测、真实 app-server、8 项界面回归和生产构建通过。会话启动/回合流/自动标题与历史领域迁移仍未完成。

- 当前增量计划：将初始自动标题同步接入 ThreadStore 的会话锁，阻止与手动重命名并发；远端创建等待期间已确认的本地手动名称优先，标题同步失败继续保留消息发送流程。

- 初始标题同步协调已交付（82251f7）：接入 ThreadStore 共享锁并优先确认过的手动名称，验收修正别名间的新旧顺序；13 项测试含真实 app-server、三种标题时序界面场景、重命名回归及生产构建通过。

- 当前增量计划：生成完整桌面包文件清单并验证，交付 Windows 便携 ZIP 与 SHA-256 校验文件；验证解压后的真实 Electron 启动及持久化。签名安装器、干净 Windows VM 和多平台发布继续待办。

- Windows 便携 ZIP 发布已交付（ca8a585）：完整目录 832 文件核验、ZIP 校验与解压后真实 Electron 验收通过；补齐发布说明和自动验收入口。签名安装器、许可审计、干净 VM 及其他平台仍待完成。

- 当前增量计划：构建时记录实际前端模块的第三方依赖与许可文本，桌面打包汇总原生 npm、Electron、Node 和 Codex 许可索引，生成可读声明及机器清单；未覆盖的 Rust/native 传递依赖明确列为待审计。

- 第三方组件许可清单已交付（bd393e9）：前端/桌面 26 个 npm 组件及 Electron/Node/Codex 索引进入完整性清单；8 项测试、新 ZIP 解压覆盖核对与真实 Electron 验收通过。Rust/native 传递依赖审计继续待办。

- 当前增量计划：自动化仓库承接按任务身份的请求互斥，页面卸载重进仍防止重复写入，创建操作单独防重；确认/失败释放锁，任务执行期仍允许正常停止。

- 自动化跨页面请求互斥已交付（07d8243）：8 项仓库单测、重进页面交叉操作保护、既有写操作界面和真实调度器完整回归通过；锁止于请求确认，不阻止运行中的任务接受停止操作。

- 当前增量计划：自动化运行历史按当前搜索/状态筛选批量导出已结束记录，包含运行身份与历史配置，固定点击时快照并提供失败重试及取消处理；通过真实保存接口和界面验收后提交。

- 筛选运行历史批量导出已交付（ad7e800）：保留点击时快照、运行身份和历史配置；4 项测试、界面到真实文件的保存/取消/重试验收、真实调度器回归及生产构建通过。范围为现存记录，不含运行中或已清理历史。

- 当前增量计划：Git 提交说明搜索覆盖所选分支完整历史，使用普通文本匹配和固定提交快照分页；提交详情/返回保留筛选，清除或换分支重置页码。真实 Git 验证后提交，再进行界面验收。

- Git 提交说明搜索已交付（64f718d）：所选分支完整说明固定文本搜索和锚定分页；验收修正详情返回保持快照。真实 Git、真实仓库界面、原历史回归及构建通过。

- 当前增量计划：历史提交详情加入 Felix 评审草稿，附完整提交身份、仓库与差异，可填写重点；追加已有草稿，大差异明确截断，读取失败或等待时禁止加入。

- 历史提交评审草稿已交付（f4302b1）：完整提交/补丁与可选重点追加已有草稿；验收修正连续点击防重和 Unicode 截断。3 项单测、真实 Git 界面/刷新持久化、历史搜索回归及构建通过。

- 当前增量计划：ThreadStore 承接服务端恢复读取，返回校验且独立的消息/运行状态/配置快照，界面保持迟到结果隔离和草稿/队列恢复保护，移除恢复路径对原始传输载荷的依赖。

- ThreadStore 恢复快照已交付（2fec773）：统一读取/身份校验/配置归一和历史隔离，主界面使用领域快照；14 项测试含真实 app-server、四项恢复相关界面回归及生产构建通过。启动和回合流等仍待迁移。

- 当前增量计划：新会话创建迁入 ThreadStore，校验创建响应后再绑定远端身份，按本地会话防重复创建，保留请求参数快照及失败重试/草稿行为；验收真实后端和异常响应界面。

- ThreadStore 新会话创建已交付（f48f205）：校验远端身份后绑定，按本地会话防重并保留失败草稿；4 项测试含真实 app-server、三项浏览器回归及构建通过。回合发送/流仍待继续拆分。

- 当前增量计划：直接发送、排队发送和追加指令使用统一 TurnCommands 确认边界，校验回合身份/状态并按会话防重复等待；坏确认保留草稿或暂停队列，避免误判成功后丢输入。

- 回合确认边界已交付（385629b）：直接发送/队列/追加指令统一校验与等待互斥；坏确认保留草稿或暂停队列，4 项测试含真实后端、三项浏览器回归及构建通过。流事件状态机仍独立维护。

- 当前增量计划：整段会话和指定回复分叉统一经 ThreadStore 校验，拒绝异常或原会话身份，等待期间与同源写操作互斥；保留原会话及失败重试，并以真实后端和界面双层验收。

- ThreadStore 会话分叉已交付（d634594）：整段及回复分叉统一身份/权限快照与同源写操作互斥；12 项测试含真实 app-server、三项界面验收及生产构建通过。坏响应保留原历史和草稿，等待防重、导航保护与有效重试通过；本轮验收未发现需修改的产品代码。

- 当前增量计划：完整历史读取及旧回复回合定位迁入 ThreadStore，统一历史校验、分页快照及失败处理，保持进度/取消、迟到结果隔离和未知条目兼容；真实 app-server 与浏览器验收后分别提交。

- ThreadStore 历史读取已交付（5ea7e17）：完整历史查找、Markdown 导出及旧回复回合定位经统一会话层，逐页复制并在返回前校验。22 项测试含真实 app-server、三项浏览器验收及生产构建通过；验收更新导出有效数据并新增坏条目不保存/保留消息/重试断言。

- 当前增量计划：已有会话渠道切换迁入 ThreadStore，校验确认会话/渠道/模型后应用配置，共享同源写操作互斥，坏响应保留原设置与草稿，单测及界面验收后分别提交推送。

- ThreadStore 渠道切换已交付（fefcaff）：确认会话/渠道/模型身份后更新本地配置，与同源写操作互斥。17 项测试、三项界面验收及生产构建通过；异常确认保留原设置、历史及草稿，有效重试及导航隔离通过。权限变更和流事件仍待继续收拢。

- 当前增量计划：权限更新经 ThreadStore 互斥与实际权限确认，拒绝空通知或未应用目标沙箱/审批策略/审批方的结果，保留全局实际设置通知，真实后端及异常界面验收后分别提交。

- ThreadStore 权限更新已交付（26cae93）：有效通知确认目标沙箱/审批策略/审批方，共享同源操作锁。7 项测试含两种 Windows 配置的真实 app-server、两项界面验收及生产构建通过；验收修正旧恢复失败文案断言，空/不匹配通知保留草稿且不记录目标已应用。

- 当前增量计划：回合开始/完成/文本增量进入状态更新前校验身份与状态，移除缺失会话身份时落到当前会话的行为，隔离已结束回合迟到事件及跨回合增量，队列和多会话界面验收后分别提交。

- 回合生命周期通知入口已交付（3a9a8c0）：开始/完成/文本增量要求有效身份与状态，拒绝结束回合迟到事件及跨回合增量，不再回退到当前会话。2 项单测、三项浏览器验收及生产构建通过；工具/错误等其他通知领域仍待继续拆分。

- 当前增量计划：工具事件在写入前检查条目身份、同条目类型和回合，已结束工具拒绝迟到开始/输出/补丁；坏增量不创建空记录或污染上一条工具，保留权威完成结果，单测与界面验收后分别提交。

- 工具增量边界已交付（76336dd）：有效条目身份、类型/回合一致性及已结束工具保护，坏输出/补丁不创建或污染记录。15 项测试、两项浏览器验收及生产构建通过；验收更新旧工具卡片选择器和恢复夹具，最终输出、补丁及刷新持久化通过。

- 当前增量计划：助手回复统一处理增量和完成事件，最终文本替换临时文本，漏增量时补建，完成后拒绝迟到追加；校验会话/回合/条目身份并避免重复回复，单测和浏览器验收后分别提交。

- 助手最终回复补全已交付（59936ff）：完成文本替换临时增量、无增量补建、重复完成去重及完成后迟到增量保护。4 项测试、三项浏览器验收及生产构建通过；验收修正刷新测试初始化，最终内容刷新保留。

- 当前增量计划：计划进度整体验证并复制步骤，避免坏步骤被静默丢弃；计划正文完成事件校验身份与文本，拒绝当前活动回合外的迟到正文和条目冲突，验收后分别提交。

- 计划事件保护已交付（3a63c2d）：进度整体验证、独立步骤快照及计划正文身份/文本校验，活动回合隔离与去重。5 项测试、两项浏览器验收及生产构建通过；坏事件保留原计划，正常计划审核到执行流程通过。
