const {chromium}=require('playwright');const assert=require('node:assert/strict');(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const page=await browser.newPage();await page.addInitScript(()=>{localStorage.setItem('codex-desktop-state-v1',JSON.stringify({activeProjectId:'p',projects:[{id:'p',path:'D:/repo'}],threads:[]}));window.desktop={workspaceGit:async input=>{if(input.action==='status')return{ok:true,result:{root:'D:/repo',branch:'main',stashAvailable:false,files:[]}};return{ok:true,result:{}};}}});await page.goto(process.env.FELIX_TEST_URL||'http://127.0.0.1:5318');await page.getByRole('button',{name:'查看 Git 变更',exact:true}).click();const restore=page.getByRole('button',{name:'恢复最近暂存',exact:true});await restore.waitFor();assert.ok(await restore.isDisabled());console.log('PASS: restore stash disabled when status reports no stash');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1});
