const { chromium } = require('playwright');
const assert = require('node:assert/strict');
(async () => {
  const browser = await chromium.launch({ channel: 'msedge', headless: true });
  try {
    const page = await browser.newPage();
    await page.addInitScript(() => {
      localStorage.setItem('codex-desktop-state-v1', JSON.stringify({ model: 'test', projects: [], threads: [] }));
      const listeners = new Set(); window.__notify = event => listeners.forEach(fn => fn(event));
      window.__calls = []; window.__opened = []; window.__failReload = true;
      window.__badStatus = '';Object.defineProperty(navigator,'clipboard',{value:{writeText:async text=>{window.__copied=text;}}});
      window.desktop = { providerStatus: async () => ({ keyConfigured: true }), listModels: async () => ({ ok: true, models: ['test'] }), openExternal: async url => window.__opened.push(url) };
      window.codex = { connect: async () => ({ ok: true }), notify: async () => ({}), request: async (method, params) => {
        window.__calls.push({ method, params });
        if (method === 'thread/start') return { ok: true, result: { thread: { id: 'resource-chat', turns: [] } } };
        if (method === 'turn/start') return { ok: true, result: { turn: { id: 'resource-turn', status: 'completed' } } };
        if (method === 'mcpServerStatus/list' && params.detail === 'full') return { ok: true, result: { data: [{ name: 'local', authStatus: 'unsupported', resources: [{ uri: 'fixture://readme', name: 'Readme' }], resourceTemplates: [{ uriTemplate: 'fixture://notes/{id}', name: 'Notes' }] }] } };
        if (method === 'mcpServer/resource/read') { if(window.__malformedResource)return {ok:true,result:{contents:[null]}};
          if (params.uri.startsWith('fixture://cancel/')) return await new Promise((resolve, reject) => { (window.__pendingResources ||= {})[params.uri] = { resolve, reject }; });
          if (params.uri === 'fixture://slow') return await new Promise(resolve => { window.__resolveResource = () => resolve({ ok: true, result: { contents: [{ uri: params.uri, text: 'Stale resource result' }] } }); });
          if (!window.__resourceRetried) { window.__resourceRetried = true; return { ok: false, error: 'Resource temporarily unavailable' }; }
          return { ok: true, result: { contents: [{ uri: params.uri, text: '<script>unsafe()</script>Resource text' }] } };
        }
        if (method === 'mcpServerStatus/list') { if(window.__badStatus==='crossPage')return {ok:true,result:{data:[{name:'cloud',authStatus:'unsupported'}],nextCursor:params.cursor?null:'next'}}; if(window.__badStatus==='endless')return {ok:true,result:{data:[],nextCursor:String(Number(params.cursor||0)+1)}}; if(window.__badStatus==='duplicate') return {ok:true,result:{data:[{name:'cloud',authStatus:'unsupported'},{name:'cloud',authStatus:'unsupported'}]}}; if(window.__badStatus==='broken') return {ok:true,result:{data:[{name:'cloud',authStatus:'unsupported',tools:[]} ]}}; return { ok: true, result: params.cursor ? { data: [{ name: 'local', authStatus: 'unsupported', runtimeStatus: 'connected', tools: { read: { annotations:{readOnlyHint:true,destructiveHint:false,openWorldHint:false}, description:'Inspect workspace documents', inputSchema:{type:'object',properties:{path:{type:'string',description:'<script>literal</script>'}},required:['path']},outputSchema:{type:'object',properties:{text:{type:'string'}}} } } }] } : { data: [{ name: 'cloud', authStatus: 'notLoggedIn', runtimeStatus: 'authenticationRequired', toolsError: 'Authentication needed', tools: {} }], nextCursor: 'page2' } }; }
        if (method === 'mcpServer/oauth/login') { if(window.__delayLogin) await new Promise(resolve=>{window.__finishLogin=resolve;}); if (window.__early) window.__notify({ method: 'mcpServer/oauthLogin/completed', params: { name: 'cloud', success: true, threadId: null } }); if(window.__failEarly)return {ok:false,error:'Obsolete login failure'}; return { ok: true, result: { authorizationUrl: 'https://example.com/login?state=test' } }; }
        if (method === 'config/mcpServer/reload' && window.__failReload) return { ok: false, error: 'Reload failed' };
        return { ok: true, result: { data: [], marketplaces: [] } };
      }, onNotification: fn => { listeners.add(fn); return () => listeners.delete(fn); }, onServerRequest: () => () => {}, onClosed: () => () => {}, onError: () => () => {}, onStderr: () => () => {} };
    });
    await page.goto(process.env.FELIX_TEST_URL || 'http://127.0.0.1:5318');
    await page.getByRole('textbox', { name: '消息', exact: true }).fill('Existing resource draft');
    await page.getByRole('button', { name: '插件', exact: true }).click();
    await page.getByRole('button', { name: '管理 MCP 服务' }).click();
    await page.getByRole('heading', { name: 'local', exact: true }).waitFor();
    const local=page.getByRole('region',{name:'local',exact:true});await local.getByText('工具 (1)',{exact:true}).click();await local.getByText('read · 输入参数',{exact:true}).click();
    await local.getByText('服务端声明（不代表权限限制）：只读：是 · 可能破坏数据：否 · 可能访问外部系统：否',{exact:true}).waitFor();assert.equal(await local.locator('script').count(),0);await local.getByRole('button',{name:'复制 read 输入参数',exact:true}).click();
    assert.deepEqual(JSON.parse(await page.evaluate(()=>window.__copied)),{type:'object',properties:{path:{type:'string',description:'<script>literal</script>'}},required:['path']});
    await local.getByText('read · 输出结构',{exact:true}).click();await local.getByRole('button',{name:'复制 read 输出结构',exact:true}).click();assert.equal(JSON.parse(await page.evaluate(()=>window.__copied)).properties.text.type,'string');

    const search=page.getByRole('searchbox',{name:'搜索 MCP 服务和工具',exact:true});
    await search.fill('  READ  ');assert.equal(await page.getByRole('heading',{name:'cloud',exact:true}).count(),0);await local.getByText('read · 输入参数',{exact:true}).waitFor();
    await search.fill('documents');await page.getByRole('heading',{name:'local',exact:true}).waitFor();
    await search.fill('CLOUD');await page.getByRole('heading',{name:'cloud',exact:true}).waitFor();assert.equal(await page.getByRole('heading',{name:'local',exact:true}).count(),0);
    await search.fill('missing-tool');await page.getByText('没有匹配的 MCP 服务或工具。',{exact:true}).waitFor();
    await page.getByRole('button',{name:'清空 MCP 搜索',exact:true}).click();await page.getByRole('heading',{name:'local',exact:true}).waitFor();
    const emitStartup=(params)=>page.evaluate(params=>window.__notify({method:'mcpServer/startupStatus/updated',params}),params);
    await emitStartup({threadId:'other',name:'cloud',status:'failed',error:'Wrong thread'});assert.equal(await page.getByText(/Wrong thread/).count(),0);
    await emitStartup({threadId:null,name:'cloud',status:'starting'});await page.getByText('cloud · 正在启动',{exact:true}).waitFor();
    await emitStartup({threadId:null,name:'cloud',status:'failed',error:'Expired <script>literal</script>',failureReason:'reauthenticationRequired'});
    const startup=page.getByRole('region',{name:'MCP 启动通知',exact:true});await startup.getByRole('alert').waitFor();assert.equal(await startup.locator('script').count(),0);
    const before=await page.evaluate(()=>window.__calls.filter(call=>call.method==='mcpServerStatus/list').length);
    await emitStartup({threadId:null,name:'cloud',status:'ready'});await startup.getByText('cloud · 已就绪',{exact:true}).waitFor();assert.equal(await startup.getByRole('alert').count(),0);
    await page.waitForFunction(before=>window.__calls.filter(call=>call.method==='mcpServerStatus/list').length>before,before);
    await page.evaluate(()=>{window.__delayLogin=true;});
    await page.getByRole('button', { name: '登录 cloud', exact: true }).click();
    await page.waitForFunction(()=>!!window.__finishLogin);
    await emitStartup({threadId:null,name:'local',status:'ready'});
    await page.evaluate(()=>{window.__delayLogin=false;window.__finishLogin();});

    await page.getByRole('button', { name: '打开 cloud 登录页面' }).click();
    assert.deepEqual(await page.evaluate(() => window.__opened), ['https://example.com/login?state=test']);
    await page.evaluate(() => window.__notify({ method: 'mcpServer/oauthLogin/completed', params: { name: 'cloud', success: true, threadId: null } }));
    await page.getByRole('status').filter({ hasText: 'cloud 登录成功' }).waitFor();
    assert.equal(await page.getByRole('button', { name: '打开 cloud 登录页面' }).count(), 0);
    await page.getByRole('button', { name: '重新加载 MCP 配置' }).click();
    await page.getByRole('alert').filter({ hasText: 'Reload failed' }).waitFor();
    await page.evaluate(() => { window.__failReload = false; });
    await page.getByRole('button', { name: '重新加载 MCP 配置' }).click();
    await page.getByRole('status').filter({ hasText: '已请求重新加载' }).waitFor();
    assert.ok(await page.evaluate(() => window.__calls.some(call => call.params?.cursor === 'page2')));
    await page.evaluate(()=>{window.__badStatus='crossPage';});await page.getByRole('button',{name:'刷新 MCP 状态',exact:true}).click();await page.getByRole('alert').filter({hasText:'MCP 跨页服务重复'}).waitFor();
    assert.equal(await page.getByRole('heading',{name:'cloud',exact:true}).count(),1);await page.getByRole('heading',{name:'local',exact:true}).waitFor();
    const endlessBefore=await page.evaluate(()=>window.__calls.filter(call=>call.method==='mcpServerStatus/list').length);
    await page.evaluate(()=>{window.__badStatus='endless';});await page.getByRole('button',{name:'刷新 MCP 状态',exact:true}).click();await page.getByRole('alert').filter({hasText:'MCP 目录分页过多'}).waitFor();
    assert.equal(await page.evaluate(()=>window.__calls.filter(call=>call.method==='mcpServerStatus/list').length)-endlessBefore,100);await page.getByRole('heading',{name:'local',exact:true}).waitFor();
    await page.evaluate(()=>{window.__badStatus='duplicate';});
    await page.getByRole('button',{name:'刷新 MCP 状态',exact:true}).click();
    await page.getByRole('alert').filter({hasText:'MCP 状态条目格式无效'}).waitFor();
    await page.getByRole('heading',{name:'cloud',exact:true}).waitFor();
    await page.evaluate(()=>{window.__badStatus='broken';});
    await page.getByRole('button',{name:'刷新 MCP 状态',exact:true}).click();
    await page.getByRole('alert').filter({hasText:'MCP 状态条目格式无效'}).waitFor();
    await page.evaluate(()=>{window.__badStatus='';});
    await page.getByRole('button',{name:'刷新 MCP 状态',exact:true}).click();
    await page.getByRole('heading',{name:'cloud',exact:true}).waitFor();
    await page.evaluate(() => { window.__early = true;window.__failEarly=true; });
    await page.getByRole('button', { name: '登录 cloud', exact: true }).click();
    await page.getByRole('status').filter({ hasText: 'cloud 登录成功' }).waitFor();
    await page.waitForFunction(() => ![...document.querySelectorAll('button')].find(button => button.textContent === '登录 cloud')?.disabled);
    assert.equal(await page.getByRole('button', { name: '打开 cloud 登录页面' }).count(), 0);
    assert.equal(await page.getByText('Obsolete login failure',{exact:false}).count(),0);
    console.log('PASS: MCP pagination, OAuth link, completion refresh and reload retry');
    await page.getByRole('checkbox', { name: '显示资源目录' }).check();
    await page.getByRole('button', { name: 'Readme', exact: true }).click();
    await page.getByRole('alert').filter({ hasText: 'Resource temporarily unavailable' }).waitFor();
    await page.evaluate(()=>{window.__malformedResource=true;});
    await page.getByRole('button', { name: '读取资源', exact: true }).click();await page.getByRole('alert').filter({hasText:'MCP 资源内容格式无效'}).waitFor();assert.equal(await page.getByLabel('资源内容').count(),0);
    await page.evaluate(()=>{window.__malformedResource=false;});
    await page.getByRole('button', { name: '读取资源', exact: true }).click();
    await page.locator('.tool-output').filter({ hasText: '<script>unsafe()</script>Resource text' }).waitFor();
    assert.equal(await page.locator('[aria-label="资源内容"] script').count(), 0);
    await page.getByText('资源模板 (1)', { exact: true }).click();
    await page.getByText('Notes', { exact: true }).click();
    const beforeTemplate = await page.evaluate(() => window.__calls.filter(call => call.method === 'mcpServer/resource/read').length);
    await page.getByRole('textbox', { name: 'Notes 参数 id', exact: true }).fill('a/b 中文');
    const expandedUri = 'fixture://notes/a%2Fb%20%E4%B8%AD%E6%96%87';
    assert.equal(await page.getByLabel('Notes 生成的 URI', { exact: true }).textContent(), expandedUri);
    assert.equal(await page.evaluate(() => window.__calls.filter(call => call.method === 'mcpServer/resource/read').length), beforeTemplate, 'Editing template must not trigger a request');
    await page.getByRole('button', { name: '读取 Notes 模板资源', exact: true }).click();
    await page.getByLabel('资源内容').getByText(expandedUri, { exact: true }).waitFor();
    assert.equal(await page.getByLabel('local 资源 URI').inputValue(), expandedUri);
    assert.equal(await page.evaluate(() => window.__calls.filter(call => call.method === 'mcpServer/resource/read').at(-1).params.uri), expandedUri);
    console.log('PASS: resource template parameters, encoded preview and explicit read');
    await page.getByLabel('local 资源 URI').fill('fixture://notes/42');
    await page.getByRole('button', { name: '读取资源', exact: true }).click();
    await page.getByLabel('资源内容').getByText('fixture://notes/42', { exact: true }).waitFor();
    const readUri = async uri => {
      await page.getByLabel('local 资源 URI').fill(uri);
      await page.getByRole('button', { name: '读取资源', exact: true }).click();
    };
    for (const outcome of ['success', 'failure']) {
      const oldUri = `fixture://cancel/${outcome}-old`;
      const newUri = `fixture://cancel/${outcome}-new`;
      await readUri(oldUri);
      await page.waitForFunction(uri => !!window.__pendingResources?.[uri], oldUri);
      await page.getByRole('button', { name: '取消等待读取', exact: true }).click();
      await page.getByRole('status').filter({ hasText: '已取消等待' }).waitFor();
      assert.equal(await page.getByLabel('资源内容').count(), 0);
      assert.equal(await page.getByRole('button', { name: 'Readme', exact: true }).isEnabled(), true);
      await readUri(newUri);
      await page.waitForFunction(uri => !!window.__pendingResources?.[uri], newUri);
      assert.equal(await page.getByText(/已取消等待/).count(), 0);
      await page.evaluate(async ({ uri, outcome }) => {
        const pending = window.__pendingResources[uri];
        if (outcome === 'success') pending.resolve({ ok: true, result: { contents: [{ uri, text: 'Cancelled stale content' }] } });
        else pending.reject(new Error('Cancelled stale failure'));
        await new Promise(resolve => setTimeout(resolve, 50));
      }, { uri: oldUri, outcome });
      await page.getByRole('status').filter({ hasText: '正在读取资源' }).waitFor();
      assert.equal(await page.getByRole('button', { name: '读取资源', exact: true }).isDisabled(), true);
      assert.equal(await page.getByText(/Cancelled stale/).count(), 0);
      await page.evaluate(uri => window.__pendingResources[uri].resolve({ ok: true, result: { contents: [{ uri, text: 'Current resource content' }] } }), newUri);
      await page.getByLabel('资源内容').getByText(newUri, { exact: true }).waitFor();
      assert.equal(await page.getByRole('button', { name: '取消等待读取', exact: true }).count(), 0);
    }
    // A cancelled response arriving after a newer read completes must also be ignored.
    await readUri('fixture://cancel/after-completion');
    await page.getByRole('button', { name: '取消等待读取', exact: true }).click();
    await readUri('fixture://notes/latest');
    await page.getByLabel('资源内容').getByText('fixture://notes/latest', { exact: true }).waitFor();
    await page.evaluate(async () => {
      window.__pendingResources['fixture://cancel/after-completion'].resolve({ ok: true, result: { contents: [{ uri: 'fixture://obsolete', text: 'Cancelled stale content' }] } });
      await new Promise(resolve => setTimeout(resolve, 50));
    });
    await page.getByLabel('资源内容').getByText('fixture://notes/latest', { exact: true }).waitFor();
    assert.equal(await page.getByText(/Cancelled stale/).count(), 0);
    console.log('PASS: cancel resource waiting, resume reads and isolate late success/failure from pending and completed reads');
    await page.getByLabel('local 资源 URI').fill('fixture://slow');
    await page.getByRole('button', { name: '读取资源', exact: true }).click();
    await page.waitForFunction(() => typeof window.__resolveResource === 'function');
    await page.getByRole('button', { name: '刷新 MCP 状态', exact: true }).click();
    await page.waitForFunction(() => ![...document.querySelectorAll('button')].find(button => button.textContent === '刷新 MCP 状态')?.disabled);
    await page.evaluate(async () => { window.__resolveResource(); await new Promise(resolve => setTimeout(resolve, 50)); });
    assert.equal(await page.getByLabel('资源内容').count(), 0, 'Refresh must discard a previous inventory read');
    await page.getByRole('button', { name: 'Readme', exact: true }).click();
    await page.getByLabel('资源内容').getByText('fixture://readme', { exact: true }).waitFor();
    await page.getByRole('checkbox', { name: '显示资源目录' }).uncheck();
    assert.equal(await page.getByLabel('资源内容').count(), 0);
    console.log('PASS: MCP resource discovery, read failure/retry, literal text, custom URI, stale-read isolation and dismissal');
    await page.getByRole('checkbox', { name: '显示资源目录' }).check();
    await page.getByRole('button', { name: 'Readme', exact: true }).click();
    await page.getByRole('button', { name: '加入聊天草稿', exact: true }).waitFor();
    await page.getByLabel('local 资源 URI').fill('fixture://not-read');
    await page.getByRole('button', { name: '加入聊天草稿', exact: true }).click();
    const draft = await page.getByRole('textbox', { name: '消息', exact: true }).inputValue();
    assert.ok(draft.startsWith('Existing resource draft\n\nMCP resource snapshot:\n'));
    assert.deepEqual(JSON.parse(draft.split('MCP resource snapshot:\n')[1]), { server: 'local', uri: 'fixture://readme', text: '<script>unsafe()</script>Resource text' });
    assert.equal(await page.evaluate(() => window.__calls.filter(call => call.method === 'turn/start').length), 0);
    await page.reload();
    assert.equal(await page.getByRole('textbox', { name: '消息', exact: true }).inputValue(), draft, 'Resource draft survives renderer reload');
    await page.waitForFunction(() => window.__calls.some(call => call.method === 'initialize'));
    await page.getByRole('status', { name: '已连接', exact: true }).waitFor();
    await page.getByRole('textbox', { name: '消息', exact: true }).press('Enter');
    await page.waitForFunction(() => window.__calls.some(call => call.method === 'turn/start'));
    const sent = await page.evaluate(() => window.__calls.find(call => call.method === 'turn/start').params.input);
    assert.deepEqual(sent, [{ type: 'text', text: draft }]);
    console.log('PASS: resource snapshot preserves draft, read provenance and reload persistence, waits for explicit send and reaches turn input');
  } finally { await browser.close(); }
})().catch(error => { console.error(error); process.exitCode = 1; });
