# 会话工作区副标题验收

实现：95d497b。有 cwd 的侧栏会话显示路径副标题，悬停提供完整路径，aria-describedby 为同名会话提供可访问描述。无工作区会话保留 28px 行高，有工作区使用 44px，长路径省略显示。

通过：thread-workspace-label-ui.cjs（同名区分、导航、置顶、1280/600px 边界）、thread-list-ui.cjs（搜索分页）和生产构建。检查了窄窗口截图，未发现新增标签与按钮重叠。

残余测试问题：sidebar-ui.cjs 未通过，旧断言遗漏设置入口且依赖旧会话顺序；临时修正夹具后长标题滚动检查通过，但后续欢迎页检查因 .welcome 不存在而失败。已撤回临时夹具修改，未把该测试记为通过。新路径副标题已由独立测试覆盖；完整侧栏旧测试需另行维护。
