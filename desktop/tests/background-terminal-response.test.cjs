const {test}=require('node:test');const assert=require('node:assert/strict');
const {parseBackgroundTerminalPage,parseTermination}=require('../src/backgroundTerminalResponse.ts');
test('validates and clones background process pages',()=>{const input={data:[{processId:'p',command:'npm test',cwd:'D:/w',osPid:4,cpuPercent:1.2,rssKb:8}],nextCursor:'n'};const page=parseBackgroundTerminalPage(input);page.data[0].command='changed';assert.equal(input.data[0].command,'npm test');assert.equal(page.nextCursor,'n');});
test('rejects malformed and duplicate process entries',()=>{for(const value of [null,{}, {data:{}},{data:[],nextCursor:{}},{data:[null]},{data:[{processId:'',command:'x',cwd:'D:/w'}]},{data:[{processId:'p',command:'',cwd:'D:/w'}]},{data:[{processId:'p',command:'x',cwd:''}]},{data:[{processId:'p',command:'x',cwd:'D:/w',cpuPercent:-1}]},{data:[{processId:'p',command:'x',cwd:'D:/w'},{processId:'p',command:'y',cwd:'D:/w'}]}])assert.throws(()=>parseBackgroundTerminalPage(value),/后台命令/);});
test('validates termination envelopes',()=>{assert.equal(parseTermination({terminated:true}),true);for(const value of [null,{}, {terminated:1},{terminated:null}])assert.throws(()=>parseTermination(value),/终止响应/);});
