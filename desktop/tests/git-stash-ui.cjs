const {chromium}=require('playwright');const assert=require('node:assert/strict');(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const page=await browser.newPage();await page.addInitScript(()=>{localStorage.setItem('codex-desktop-state-v1',JSON.stringify({activeProjectId:'p',projects:[{id:'p',path:'D:/repo'}],threads:[]}));window.__calls=[];window.desktop={workspaceGit:async input=>{window.__calls.push(input);if(input.action==='status')return{ok:true,result:{root:'D:/repo',branch:'main',head:'abc',files:[{path:'a.txt',index:' ',working:'M',untracked:false}]}};if(input.action==='stash')return{ok:false,error:'工作区发生变化，请刷新 Git 变更'};return{ok:true,result:{}};}}});await page.goto(process.env.FELIX_TEST_URL||'http://127.0.0.1:5318');await page.getByRole('button',{name:'查看 Git 变更',exact:true}).click();await page.getByRole('button',{name:'暂存工作区',exact:true}).click();await page.getByRole('alert').getByText('工作区发生变化，请刷新 Git 变更',{exact:true}).waitFor();assert.ok(await page.getByRole('button',{name:'恢复最近暂存',exact:true}).isEnabled());console.log('PASS: stash controls report stale-worktree errors without hiding restore');}finally{await browser.close();}})().catch(error=>{console.error(error);process.exitCode=1;});
